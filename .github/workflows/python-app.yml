- name: Ejecutar Python inline
  shell: python
  run: |
    import os
    import sys
    
    print("Hola desde GitHub Actions")
    print(f"Directorio actual: {os.getcwd()}")
    
    # Tu lógica aquí
    datos = [1, 2, 3, 4, 5]
    print(f"Sumatoria: {sum(datos)}")
